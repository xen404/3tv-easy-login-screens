
const path = /^\/health\/?$/

module.exports = () => async (ctx, next) => {
  if (path.test(ctx.path)) {
    ctx.body = { status: 'UP',
      maintenanceFlag: { status: 'UP' },
      maintenanceFile: { status: 'UP' } }
  } else {
    await next()
  }
}
